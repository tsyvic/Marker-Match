import { styled } from '@mui/material';

export const HeaderContainer = styled('div')(() => ({
  backgroundImage: 'url(/assets/homePagePlaceholder.jpg)',
  height: '100vh',
  backgroundSize: 'cover',
  backgroundRepeat: 'no-repeat',
  backgroundPosition: '70% 50%',
}));
