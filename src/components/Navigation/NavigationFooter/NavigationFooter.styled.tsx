import { styled } from '@mui/material';

export const NavigationFooter = styled('div')(({ theme }) => ({
  display: 'flex',
  flexDirection: 'column',
  marginTop: 'auto',
  marginBottom: theme.spacing(4),
}));
